"""TextShield utility package: small, dependency-free helpers.

Each module here is a leaf: it must never import from app services,
routes or ML/RAG packages, so utilities stay reusable everywhere.
"""
