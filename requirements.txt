# TextShield core dependencies
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.6
jinja2>=3.1
python-multipart>=0.0.9
python-dotenv>=1.0
joblib>=1.3
numpy>=1.26
scikit-learn>=1.4
requests>=2.31
pandas>=2.2

# Vector database (primary RAG backend)
chromadb>=0.4.24

# Sentence embeddings (primary embedding provider).
# NOTE: installs PyTorch. If you prefer a lighter setup, the app automatically
# falls back to a dependency-free hashing embedder when these are missing.
sentence-transformers>=2.7

# Web app serving for the dashboard
# (Python 3.13+ needs a recent aiofiles for StaticFiles)
aiofiles>=23.2

# Testing
pytest>=8.0
httpx>=0.27